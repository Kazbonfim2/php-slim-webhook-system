services:
  app:
    build: .
    ports:
      - "8081:8080"
    volumes:
      - .:/app
    working_dir: /app
    command: sh -c "composer install --no-interaction && php -d opcache.enable=0 -S 0.0.0.0:8080 -t public public/index.php"
    env_file: .env
